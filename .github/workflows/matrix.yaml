name: build
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    outputs:
       matrix: ${{ steps.setmatrix.outputs.matrix }}
    steps:
    - name: checkout source code
      uses: actions/checkout@v1
    - id: setmatrix
      run: |
        shopt -s nullglob
        matrixArray=$(find ./lambdas -name '*.js') # Creates array of all files .js withing lambdas
        echo $array | \ # Start Generate Json String
        jq --slurp --raw-input 'split(" ")[:-1]' | \
        jq  "{\"lambdafile\": .[] }" | \
        jq -s "{ \"include\": . }" | \
        jq tostring > tmp # End Generate Json String
        matrixStringifiedObject=`cat tmp` # Use this as jq @sh not working right
        echo "::set-output name=matrix::$matrixStringifiedObject"
        #echo "::set-output name=matrix::{\"include\":[{\"lambdafile\":\"./example1/lambdafile1.js\"},{\"lambdafile\":\"./example2/lambdafile2.js\"}]}"
  job2:
    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{fromJson(needs.job1.outputs.matrix)}}
    steps:
     - name: checkout source code
       uses: actions/checkout@v1
     - run: echo ${{ matrix.lambdafile }}
